# VPC Templates

This repository contains reusable Terraform templates for deploying basic AWS VPC architectures.

Each subfolder includes a self-contained VPC configuration with variable tagging support and options for multiple environments and AWS regions.